import 'package:flutter/material.dart';

class Alpha extends StatelessWidget {
  static const routeName = 'alpha';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Alpha Page'),
    );
  }
}
